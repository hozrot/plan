import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function note() {
  return (
    <View>
      <Text>
        After Creating a page we can create a template of code by typing rfs and tab.
        Then we need a declare that page source into app.js 
        also import that page where you want to add tab Navigator
      </Text>
    </View>
  )
}

const styles = StyleSheet.create({})